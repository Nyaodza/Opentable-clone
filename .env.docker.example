# OpenTable Clone - Docker Environment Variables
# Copy this file to .env and fill in the required values
# NEVER commit the .env file to version control

# ===========================================
# REQUIRED - Application will not start without these
# ===========================================

# Database credentials (use strong passwords in production!)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_super_secure_password_here
POSTGRES_DB=opentable_clone

# JWT Secret - Generate with: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_at_least_32_chars_long_change_in_production

# NextAuth Secret - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_at_least_32_chars_long

# Redis password (use strong password in production!)
REDIS_PASSWORD=your_redis_password_here

# pgAdmin password (only needed if using dev-tools profile)
PGADMIN_PASSWORD=your_pgadmin_password_here

# ===========================================
# OPTIONAL - Defaults provided
# ===========================================

# Environment
NODE_ENV=development

# URLs (change for production)
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3001/api
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001
NEXTAUTH_URL=http://localhost:3000

# JWT expiration
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# Email configuration (defaults to MailDev for development)
EMAIL_USER=noreply@opentable-clone.com
EMAIL_PASS=test
EMAIL_FROM=noreply@opentable-clone.com

# pgAdmin (only used with dev-tools profile)
PGADMIN_EMAIL=admin@example.com

# ===========================================
# PRODUCTION SECURITY - Disable external ports
# ===========================================

# Set to empty or internal-only port in production
# POSTGRES_EXTERNAL_PORT=
# REDIS_EXTERNAL_PORT=
# ELASTICSEARCH_EXTERNAL_PORT=
# PGADMIN_EXTERNAL_PORT=

# Enable Elasticsearch security in production
# ELASTICSEARCH_SECURITY_ENABLED=true
# ELASTICSEARCH_PASSWORD=your_es_password

# ===========================================
# Quick Start for Development
# ===========================================
# 1. Copy this file: cp .env.docker.example .env
# 2. Generate secrets:
#    - JWT_SECRET: openssl rand -base64 64
#    - NEXTAUTH_SECRET: openssl rand -base64 32
# 3. Set passwords for POSTGRES_PASSWORD, REDIS_PASSWORD, PGADMIN_PASSWORD
# 4. Run: docker-compose up -d
# 5. For dev tools (pgAdmin): docker-compose --profile dev-tools up -d
